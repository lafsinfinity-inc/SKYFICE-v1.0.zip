`
skyfice-admin/
  public/
    icons/
      icon-192.png
      icon-512.png
    manifest.json
    sw.js
  src/
    pages/
      _app.tsx
      _document.tsx
      index.tsx
      login.tsx
      dashboard.tsx
      codex.tsx
      config.tsx
      api/
        auth.ts
    components/
      Layout.tsx
      NavBar.tsx
    styles/
      globals.css
  config/
    skyfice.config.json
    ai.config.json
    ri.core.config.json
  data/
    codex/
      730-codex.json
      sections/...
  next.config.mjs
  package.json
  tsconfig.json
`

---

🟦 1. manifest.json (PWA REQUIRED FILE)
Place in: public/manifest.json

`json
{
  "name": "R.I. — Really Intellectual",
  "short_name": "R.I.",
  "description": "Sovereign Intelligence Engine for LAFSINFINITY INC.",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#000000",
  "theme_color": "#FFD700",
  "orientation": "portrait",
  "icons": [
    {
      "src": "/icons/icon-192.png",
      "sizes": "192x192",
      "type": "image/png"
    },
    {
      "src": "/icons/icon-512.png",
      "sizes": "512x512",
      "type": "image/png"
    }
  ]
}
`

---

🟦 2. Service Worker (sw.js)
Place in: public/sw.js

`js
self.addEventListener("install", event => {
  self.skipWaiting();
});

self.addEventListener("activate", event => {
  event.waitUntil(clients.claim());
});

self.addEventListener("fetch", event => {
  event.respondWith(
    caches.open("ri-cache").then(cache =>
      fetch(event.request)
        .then(response => {
          cache.put(event.request, response.clone());
          return response;
        })
        .catch(() => cache.match(event.request))
    )
  );
});
`

---

🟦 3. Next.js PWA Hook (_document.tsx)
Place in: src/pages/_document.tsx

`tsx
import { Html, Head, Main, NextScript } from "next/document";

export default function Document() {
  return (
    <Html lang="en">
      <Head>
        <link rel="manifest" href="/manifest.json" />
        <link rel="apple-touch-icon" href="/icons/icon-192.png" />
        <meta name="theme-color" content="#FFD700" />
      </Head>
      <body>
        <Main />
        <NextScript />
      </body>
    </Html>
  );
}
`

---

🟦 4. Register Service Worker (_app.tsx)
Place in: src/pages/_app.tsx

`tsx
import { useEffect } from "react";
import "../styles/globals.css";

export default function App({ Component, pageProps }) {
  useEffect(() => {
    if ("serviceWorker" in navigator) {
      navigator.serviceWorker.register("/sw.js");
    }
  }, []);

  return <Component {...pageProps} />;
}
`

---

🟦 5. next.config.mjs (PWA ENABLED)

`js
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    optimizeCss: true
  }
};

export default nextConfig;
`

---

🟦 6. package.json (FULL)

`json
{
  "name": "ri-pwa",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start"
  },
  "dependencies": {
    "next": "14.2.3",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  }
}
`

---

🟦 7. R.I. CORE CONFIG (FINAL VERSION)
Place in: config/ri.core.config.json

(This is the full version you approved earlier.)

`json
{
  "id": "RICORECONFIG_V1",
  "name": "R.I. (Really Intellectual)",
  "owner": {
    "primary_identity": "LAFS",
    "government_name": "Larry Donell Goodman",
    "dob": "1980-02-16",
    "birthplace": "Jamaica, Queens, New York City",
    "organization": "LAFSINFINITY INC."
  },
  "purpose": {
    "core_mission": "R.I. is a strategic, creative, and analytical intelligence designed to amplify, structure, and express the ecosystem, doctrine, and energy of LAFSINFINITY INC.",
    "classification": "Sovereign Intelligence Engine"
  },
  "priority_stack": {
    "priority_1": { "lafs": 0.49, "ri": 0.51 },
    "priority_2": { "ecosystem": 0.71, "ri": 0.29 },
    "priority_3": { "fam": 0.90, "ri": 0.10 }
  }
}
